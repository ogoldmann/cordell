version: "2"

sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "internal/infra/postgres/queries"
    gen:
      go:
        package: "db"
        out: "internal/infra/postgres/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true