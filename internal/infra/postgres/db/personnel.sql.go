// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: personnel.sql

package db

import (
	"context"
)

const createPersonnel = `-- name: CreatePersonnel :exec
INSERT INTO personnel (
    id,
    full_name,
    alias,
    rank,
    registration_id,
    section,
    organization_unit
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7
)
`

type CreatePersonnelParams struct {
	ID               string `json:"id"`
	FullName         string `json:"full_name"`
	Alias            string `json:"alias"`
	Rank             string `json:"rank"`
	RegistrationID   string `json:"registration_id"`
	Section          string `json:"section"`
	OrganizationUnit string `json:"organization_unit"`
}

func (q *Queries) CreatePersonnel(ctx context.Context, arg CreatePersonnelParams) error {
	_, err := q.db.Exec(ctx, createPersonnel,
		arg.ID,
		arg.FullName,
		arg.Alias,
		arg.Rank,
		arg.RegistrationID,
		arg.Section,
		arg.OrganizationUnit,
	)
	return err
}

const getPersonnel = `-- name: GetPersonnel :one
SELECT
    id,
    full_name,
    alias,
    rank,
    registration_id,
    section,
    organization_unit,
    active,
    created_at,
    updated_at
FROM personnel
WHERE id = $1
`

func (q *Queries) GetPersonnel(ctx context.Context, id string) (Personnel, error) {
	row := q.db.QueryRow(ctx, getPersonnel, id)
	var i Personnel
	err := row.Scan(
		&i.ID,
		&i.FullName,
		&i.Alias,
		&i.Rank,
		&i.RegistrationID,
		&i.Section,
		&i.OrganizationUnit,
		&i.Active,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listPersonnel = `-- name: ListPersonnel :many
SELECT
    id,
    full_name,
    alias,
    rank,
    registration_id,
    section,
    organization_unit,
    active,
    created_at,
    updated_at
FROM personnel
ORDER BY created_at DESC, id DESC
LIMIT $1
`

func (q *Queries) ListPersonnel(ctx context.Context, limitCount int32) ([]Personnel, error) {
	rows, err := q.db.Query(ctx, listPersonnel, limitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Personnel{}
	for rows.Next() {
		var i Personnel
		if err := rows.Scan(
			&i.ID,
			&i.FullName,
			&i.Alias,
			&i.Rank,
			&i.RegistrationID,
			&i.Section,
			&i.OrganizationUnit,
			&i.Active,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchPersonnel = `-- name: SearchPersonnel :many
WITH search_terms AS (
    SELECT
        text_terms.search_pattern,
        digit_terms.registration_pattern
    FROM unnest($2::text[]) WITH ORDINALITY AS text_terms(search_pattern, term_position)
    JOIN unnest($3::text[]) WITH ORDINALITY AS digit_terms(registration_pattern, term_position)
        USING (term_position)
)
SELECT
    id,
    full_name,
    alias,
    rank,
    registration_id,
    section,
    organization_unit,
    active,
    created_at,
    updated_at
FROM personnel
WHERE NOT EXISTS (
    SELECT 1
    FROM search_terms
    WHERE NOT (
        full_name ILIKE search_terms.search_pattern ESCAPE '\'
        OR alias ILIKE search_terms.search_pattern ESCAPE '\'
        OR registration_id ILIKE search_terms.search_pattern ESCAPE '\'
        OR registration_id ILIKE search_terms.registration_pattern ESCAPE '\'
        OR rank ILIKE search_terms.search_pattern ESCAPE '\'
        OR section ILIKE search_terms.search_pattern ESCAPE '\'
        OR organization_unit ILIKE search_terms.search_pattern ESCAPE '\'
    )
)
ORDER BY created_at DESC, id DESC
LIMIT $1
`

type SearchPersonnelParams struct {
	LimitCount           int32    `json:"limit_count"`
	SearchPatterns       []string `json:"search_patterns"`
	RegistrationPatterns []string `json:"registration_patterns"`
}

func (q *Queries) SearchPersonnel(ctx context.Context, arg SearchPersonnelParams) ([]Personnel, error) {
	rows, err := q.db.Query(ctx, searchPersonnel, arg.LimitCount, arg.SearchPatterns, arg.RegistrationPatterns)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Personnel{}
	for rows.Next() {
		var i Personnel
		if err := rows.Scan(
			&i.ID,
			&i.FullName,
			&i.Alias,
			&i.Rank,
			&i.RegistrationID,
			&i.Section,
			&i.OrganizationUnit,
			&i.Active,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
